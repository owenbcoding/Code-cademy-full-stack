# Semantic HTML for Accessibility

Semantic elements communicate structure and meaning to browsers and assistive
technology. Prefer headings, landmarks, lists, buttons, and links over generic
`div` elements with click handlers.

