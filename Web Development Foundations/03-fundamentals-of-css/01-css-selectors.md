# CSS Selectors and Declarations

CSS separates presentation from document structure. A rule contains a
selector, declarations, and values.

## Practice

Open `practice.html` and use `styles.css` to style the page. Try changing the
font, text colour, spacing, and background colour.

