# The CSS Box Model

Every element is laid out as content, padding, border, and margin. Use
`box-sizing: border-box` when you want declared dimensions to include padding
and borders.

## Practice

Open `practice.html` and adjust the card spacing and layout in `styles.css`.

