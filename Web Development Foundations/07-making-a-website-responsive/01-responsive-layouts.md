# Responsive Layouts

Responsive sites adapt to the available viewport. Use flexible widths,
relative units, and media queries instead of designing for one fixed screen
size.

## Practice

Open `practice.html`, resize the browser, and observe the layout change at the
breakpoint in `responsive.css`.

